{
  "name": "@shck-dev/notion-mcp",
  "version": "0.1.4",
  "description": "MCP server for Notion using internal API — no workspace admin needed",
  "type": "module",
  "bin": {
    "notion-mcp": "src/server.ts"
  },
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun test",
    "typecheck": "bun tsc --noEmit"
  },
  "keywords": [
    "notion",
    "mcp",
    "model-context-protocol",
    "markdown",
    "claude"
  ],
  "license": "MIT",
  "engines": {
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "tsx": "^4.21.0"
  }
}
